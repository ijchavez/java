package Clase4.Listas;

import java.util.ArrayList;

public class Ejercicio1 {
    public static void main(String[] args){
        ArrayList<Integer> arrayPrueba = new ArrayList<Integer>();
        int num = 0;

        arrayPrueba.add(num); //siempre les tengo que pasar el elemento
        arrayPrueba.remove(num); //siempre les tengo que pasar el elemento
        arrayPrueba.clear();
        arrayPrueba.size();
        arrayPrueba.get(num); //siempre les tengo que pasar el elemento
        arrayPrueba.isEmpty();
        arrayPrueba.indexOf(num); //el contador en un for por ej


    }

}
